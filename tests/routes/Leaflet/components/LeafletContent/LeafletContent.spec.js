/*
import React from 'react'
import { LeafletContent } from 'routes/Leaflet/components/LeafletContent/LeafletContent.js'
import { shallow } from 'enzyme'

describe('(Component) LeafletContent', () => {
  const props = { title: '10.3.1 Molecular Dynamics', isFavorited: false }

  let _component
  beforeEach(() => {
    _component = shallow(
      <LeafletContent title={props.title} isFavorited={props.isFavorited} />
    )
  })

  // TODO: until further progress this component is un testable as it is a wrapper
})
*/
